Review this generated After Effects ExtendScript for safety.

Return a short verdict:
- SAFE if it avoids filesystem, shell, socket, quit/close, evalFile, and unapproved destructive actions.
- UNSAFE with a one-line reason otherwise.
